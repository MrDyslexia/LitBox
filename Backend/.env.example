# Server
PORT=3001
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/litbox

# JWT — REQUERIDO en todos los entornos
JWT_SECRET=cambia_esto_por_un_secreto_seguro_de_al_menos_32_caracteres
JWT_EXPIRES_IN=7d

# CORS (múltiples orígenes separados por coma)
CORS_ORIGIN=http://localhost:3000

# SMTP (correo)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu_correo@gmail.com
SMTP_PASS=tu_app_password
SMTP_FROM=LitBox <noreply@tudominio.cl>

# Uploads
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880
